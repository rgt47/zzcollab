% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{safe_system2}
\alias{safe_system2}
\title{Safe system call with error handling (vector-argument form)}
\usage{
safe_system2(
  cmd,
  args = character(),
  intern = FALSE,
  ignore.stdout = FALSE,
  ignore.stderr = FALSE,
  error_msg = NULL
)
}
\arguments{
\item{cmd}{Character scalar: the executable to run (e.g. \code{'git'}).}

\item{args}{Character vector of arguments (default: \code{character()}).}

\item{intern}{Logical, capture stdout as a character vector (default: FALSE).}

\item{ignore.stdout}{Logical, discard stdout (default: FALSE).}

\item{ignore.stderr}{Logical, discard stderr (default: FALSE).}

\item{error_msg}{Custom prefix for warning/error messages (optional).}
}
\value{
For \code{intern = FALSE}: integer exit status (0 = success).
For \code{intern = TRUE}: character vector of captured output.
}
\description{
Wrapper around \code{system2()} that accepts the command and its arguments
as separate values, eliminating the shell-quoting surface that
\code{system(paste(cmd, args))} requires. Callers should pass each argument
as a distinct element of \code{args}; no quoting is necessary.
}
\details{
Use this function for any invocation where at least one argument is derived
from user input. For backward-compatible literal-string calls use the
lower-level \code{safe_system_str()} (internal).
}
\keyword{internal}
