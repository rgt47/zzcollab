# Test legacy compatibility
variants:
  # New simplified format
  minimal:
    enabled: true    # Essential development environment (~800MB)
    # Full definition in variant_examples.yaml
    
  # Legacy full definition format
  legacy_custom:
    enabled: true
    base_image: "rocker/r-ver:latest"
    description: "Legacy custom variant with full definition"
    packages:
      - "renv"
      - "devtools"
      - "dplyr"
    system_deps:
      - "libxml2-dev"

build:
  variant_library: "variant_examples.yaml"