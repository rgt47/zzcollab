# Add R package to renv

Add R package to renv

## Usage

``` r
add_package(packages, update_snapshot = TRUE)
```

## Arguments

- packages:

  Character vector of package names

- update_snapshot:

  Logical, update renv.lock after installation

## Value

Logical indicating success
