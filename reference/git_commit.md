# Create and push git commit

Create and push git commit

## Usage

``` r
git_commit(message, add_all = TRUE)
```

## Arguments

- message:

  Commit message

- add_all:

  Logical, add all files (default: TRUE)

## Value

Logical indicating success
