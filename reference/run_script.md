# Execute analysis script in container

Execute analysis script in container

## Usage

``` r
run_script(script_path, container_cmd = "docker-r")
```

## Arguments

- script_path:

  Path to R script

- container_cmd:

  Container command (default: "docker-r")

## Value

Logical indicating success
